<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CRT"
             x:Class="CRT.TabResources">

	<UserControl.Styles>
		<!-- Minimal hyperlink button style -->
		<Style Selector="Button.LinkButton">
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="Foreground" Value="{DynamicResource AppThemeAccentBrush}" />
			<Setter Property="Padding" Value="8,4" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="HorizontalAlignment" Value="Left" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>

		<Style Selector="Button.LinkButton:pointerover /template/ ContentPresenter">
			<Setter Property="Background" Value="Transparent" />
		</Style>
		<Style Selector="Button.LinkButton:pointerover TextBlock">
			<Setter Property="Foreground" Value="{DynamicResource AppThemeAccentLightBrush}" />
		</Style>

		<Style Selector="Button.LinkButton:pressed /template/ ContentPresenter">
			<Setter Property="Background" Value="Transparent" />
		</Style>

		<Style Selector="Border.ResourceRow">
			<Setter Property="Background" Value="Transparent" />
		</Style>
		<Style Selector="Border.ResourceRow:pointerover">
			<Setter Property="Background" Value="{DynamicResource Table_RowHover_Bg}" />
		</Style>
	</UserControl.Styles>

	<ScrollViewer>
		<StackPanel Margin="16" Spacing="24">

			<TextBlock Text="Board specific resources for troubleshooting and information."
					   FontSize="14" />

			<!-- Local Files Section -->
			<StackPanel Spacing="8">
				<TextBlock Text="Local files"
						   FontSize="14"
						   FontWeight="Bold" />

				<Border BorderBrush="{DynamicResource Table_BorderRowLine}" BorderThickness="1">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>

						<!-- Header -->
						<Grid Grid.Row="0" Background="{DynamicResource Table_Header_Bg}">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="200" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Category" FontWeight="Bold" Padding="8,4" Foreground="{DynamicResource Table_Header_Fg}" />
							<TextBlock Grid.Column="1" Text="Name" FontWeight="Bold" Padding="8,4" Foreground="{DynamicResource Table_Header_Fg}" />
						</Grid>

						<!-- Items -->
						<ItemsControl 
							Grid.Row="1" 
							Background="{DynamicResource Table_Bg}"
							x:Name="LocalFilesItemsControl">
							<ItemsControl.ItemTemplate>
								<DataTemplate DataType="local:ResourceItem">
									<Border Classes="ResourceRow" BorderBrush="{DynamicResource Table_BorderRowLine}" BorderThickness="0,0,0,1">
										<Grid>
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="200" />
												<ColumnDefinition Width="*" />
											</Grid.ColumnDefinitions>
											<TextBlock Grid.Column="0"
													   Text="{Binding Category}"
													   Foreground="{DynamicResource Table_Fg}"
													   Padding="8,4"
													   VerticalAlignment="Center" />
											<Button Grid.Column="1"
													Classes="LinkButton"
													Command="{Binding OpenCommand}">
												<TextBlock Text="{Binding Name}" TextDecorations="Underline" />
											</Button>
										</Grid>
									</Border>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</Grid>
				</Border>
			</StackPanel>

			<!-- Web Links Section -->
			<StackPanel Spacing="8">
				<TextBlock Text="Links"
						   FontSize="14"
						   FontWeight="Bold" />

				<Border BorderBrush="{DynamicResource Table_BorderRowLine}" BorderThickness="1">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>

						<!-- Header -->
						<Grid Grid.Row="0" Background="{DynamicResource Table_Header_Bg}">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="200" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Category" Padding="8,4" Foreground="{DynamicResource Table_Header_Fg}" />
							<TextBlock Grid.Column="1" Text="Name" Padding="8,4" Foreground="{DynamicResource Table_Header_Fg}" />
						</Grid>

						<!-- Items -->
						<ItemsControl 
							Grid.Row="1" 
							x:Name="WebLinksItemsControl"
							Background="{DynamicResource Table_Bg}"
							>
							<ItemsControl.ItemTemplate>
								<DataTemplate DataType="local:ResourceItem">
									<Border Classes="ResourceRow" BorderBrush="{DynamicResource Table_BorderRowLine}" BorderThickness="0,0,0,1">
										<Grid>
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="200" />
												<ColumnDefinition Width="*" />
											</Grid.ColumnDefinitions>
											<TextBlock Grid.Column="0"
													   Text="{Binding Category}"
													   Foreground="{DynamicResource Table_Fg}"
													   Padding="8,4"
													   VerticalAlignment="Center" />
											<Button Grid.Column="1"
													Classes="LinkButton"
													Command="{Binding OpenCommand}">
												<TextBlock Text="{Binding Name}" TextDecorations="Underline" />
											</Button>
										</Grid>
									</Border>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</Grid>
				</Border>
			</StackPanel>

		</StackPanel>
	</ScrollViewer>
</UserControl>